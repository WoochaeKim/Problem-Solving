#include <iostream>
#include <algorithm>
#include <iostream>

//#define PIVOT_EXP 3

#define JUINS DUNGJUN

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	cout << JUINS;

	/*
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	
	int iExponent{};
	string strInput;

	cin >> strInput;

	int iCntLoop{ static_cast<int>(strInput.size()) };

	while (iCntLoop--)
	{
		(PIVOT_EXP <= iExponent) ? (iExponent = 0) : iExponent++;
		strInput[iCntLoop] 
	}


	reverse(strInput.begin(), strInput.end());

	int iLimitIdx{ ) };

	for (int iIdx = 0; )
	*/
	return 0;
}